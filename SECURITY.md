# Security Policy

## Supported version

安全修复只针对最新 `main` 和最新版本标签。旧版本应先升级后再复现。

## Private reporting

如果问题可能导致按键发送到错误目标、绕过人工确认、泄露终端内容或凭据，请不要创建公开 Issue。请在 GitHub 仓库的 **Security → Report a vulnerability** 中提交私密报告。

报告应包含：

- 受影响版本和提交；
- 操作系统、桌面会话和终端；
- 是否使用 Herdr，以及 Herdr 版本；
- 最小复现步骤和预期/实际行为；
- 已脱敏的日志或截图。

不要提交真实 API 密钥、访问令牌、SSH 私钥或包含私人终端内容的完整录屏。维护者确认并修复前，请避免公开披露可利用细节。
